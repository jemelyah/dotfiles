# Work git identity override, included via [includeIf] in ~/.gitconfig
# for repos under ~/rails, ~/rubygems, ~/java on the work profile.
#
# TODO (when the work machine/profile is set up): create a work Bitwarden
# item holding name/email, then replace these placeholders with e.g.
# {{ (bitwardenFields "item" "<item-name>").name.value }}
[user]
	name = TODO-work-name
	email = TODO-work-email
